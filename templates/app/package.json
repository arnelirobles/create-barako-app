{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port ${WEB_PORT:-{{WEB_PORT}}}",
    "build": "next build",
    "start": "next start --port ${WEB_PORT:-{{WEB_PORT}}}",
    "seed": "node seed/seed.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@baryodev/barako-client": "{{CLIENT_VERSION}}",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
